# AV-HuBERT 双角色 D1：150k 评测（2026-09-07）

CelebV-Dub Setting 1，213 条；EMA，seed=0，Euler/EPSS，NFE=32，sway=-1，CFG text/video=5/2，真实时长，HiFi-GAN 16 kHz。同一 clip 的 GT 音频作参考。

模型使用本项目独立配置 `src/aligndit/config/eval_celebvdub_mm_d1_hunyuan_dual_ca_allrope_avhubert_dual_role.yaml`，已核对与训练 Hydra 快照的模型结构一致。checkpoint 内部 update=150000，含双角色投影头的 EMA 参数。

Checkpoint：`/zjw524/projects/data/ckpts/AlignDiT_MMDiT_D1_HunyuanDualCA_AllRoPE_AVHuBERTDualRole_L6_W01_finetune_hifigan_16k_CelebVDub_char/model_150000.pt`。

| 指标 | 原 Hunyuan 双流 CA / AllRoPE D1 150k | 加双角色监督 150k | 差值（新−原） |
|---|---:|---:|---:|
| SIM ↑ | 0.58633 | 0.59017 | +0.00384 |
| WER ↓ | 0.06308 | 0.06644 | +0.00336 |
| EMOSIM ↑ | 0.73995 | 0.73943 | -0.00052 |
| AVSync ↑ | 0.52144 | 0.51715 | -0.00429 |

当前结果尚不支持整体提升：SIM 略升，WER 和 AVSync 变差，EMOSIM 基本持平。这是独立训练运行的描述性对照，未做多随机种子检验。WER 共 2378 个参考词，替换 59、删除 79、插入 20，总错误数 158；按 corpus 计算为 158/2378（约 6.64424%），不是逐句 WER 均值。

## 完整性与产物

逐项核验 213 个 WAV、213 个 AV-HuBERT 特征及四份各 213 条记录的 JSONL。按测试列表顺序恢复完整 clip 路径，避免跨视频同名 clip 混淆；WER 参考文本也逐条匹配。WAV 全为 16 kHz 单声道，无非有限值或全零样本；与 GT 最大时长差为 0.038125 秒。生成特征形状与 GT 一致且数值有限。独立复算四指标与汇总一致。

以下路径相对于本项目：

- 结果目录：`results/eval_celebvdub_mm_d1_hunyuan_dual_ca_allrope_avhubert_dual_role_150000/celebvdub_test_s1/seed0_euler_nfe32_hifigan_16k_ss-1.0_cfgt5.0_cfgv2.0_gt-dur/`。
- 四指标文件：上述目录内 `_sim_results.jsonl`、`_wer_results.jsonl`、`_emosim_results.jsonl`、`_avsync_results.jsonl`。
- 日志：`logs/eval_avhubert_dual_role_150k_20260907.log`。
- 复核记录（含结果文件 SHA256）：`logs/eval_avhubert_dual_role_150k_audit_20260907.json`。
- 入口：`src/aligndit/run/eval/run_celebvdub_s1_avhubert_dual_role_150k_4x4090.sh`。
- 对照证据：原项目 `AlignDiT_mmdit_base_qknorm_ca_solve_prompt_audio/logs/eval_d1_hunyuan_150k_audit_20260907.json`。

## 本次入口修正

新入口的 AVSync 阶段最初把 fairseq 导入根目录少写一层，且嘴部视频根目录少写一层 `test`，导致映射出的音频路径多出 `test`。按历史成功入口改为 `av_hubert/fairseq/fairseq` 和 `video_mouth/test/test`，逐项确认全部 213 对视频/音频相对路径一致后，补跑特征提取与 AVSync 并成功结束。前三项结果保留；此修正不改变模型或指标定义。
